@using Novel.Common.Models
@{
    ViewData["Title"] = "Detail";
    List<NomicCatalog> catalogs = ViewBag.Catalogs;
}

<div class="tab-content" id="nav-tabContent">
    <div class="tab-pane fade show active" id="pills-home" role="tabpanel" aria-labelledby="pills-home-tab">
        @foreach (var item in catalogs)
        {
            <div class="card   mb-3 border border-info rounded">
                <div class="card-body">
                    <h5 class="card-title">@item.Title</h5>
                    <a onclick="ReadDetail('@item.Url','@item.Index')" class="btn btn-outline-primary">阅  读</a>
                </div>
            </div>
        }
    </div>
</div>
<script>
    function ReadDetail(title, index) {
        window.location.href = "/Nomic/NomicContent?title=" + title + "&index=" + index;
    }
</script>
